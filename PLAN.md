# Mao 语言第一版实现计划

## 1. 项目目标

Mao 是一门编译到 Go 的静态类型语言方言，使用狸花猫作为项目形象。第一版以“减少 Go 日常语法负担，同时保持 Go 生态兼容”为目标：

- 类型采用前置写法。
- 局部变量允许直接推断类型，不需要 `var`；只有 `const` 声明常量。
- 数组、切片和字典使用统一的方括号字面量。
- 集合可以通过 Go 的 `any` 容纳不同类型的元素或值；同类型集合仍保留精确类型。
- 代码区允许部分中文标点作为 Go 标点的等价写法，并由格式化器统一输出 ASCII 标点。
- 字典项允许省略值，省略值等同于 `null`。
- 为原生字典和切片提供 `.Delete(...)`，切片按位置删除使用 `.DeleteAt(...)`。
- 提供统一的 `float` 类型，默认对应 Go `float64`。
- Mao 文件能够导入 Go 包，并与同一包内的 Go 文件互相调用。

第一版不修改 Go 编译器，也不引入自定义虚拟机。`mao` 编译器将 `.mao` 源码转换为可读的 `.go` 源码，再调用官方 Go 工具链完成构建、运行和测试。

## 2. 语言规则

### 2.1 关键字与预声明标识符

Mao 完整沿用 Go 的 25 个关键字，不增加新关键字：

```text
break       default     func        interface   select
case        defer       go          map         struct
chan        else        goto        package     switch
const       fallthrough if          range       type
continue    for         import      return       var
```

Mao 继承 Go 的预声明标识符，并作以下明确调整：

| 名称 | 分类 | 规则 |
|---|---|---|
| `null` | 预声明空值 | 替换 Go 的 `nil` |
| `float` | 预声明类型 | 固定对应 Go `float64` |
| `dict` | 预声明函数 | 把数组或切片转换为以元素为键、`null` 为值的字典 |
| `any` | 预声明类型 | 直接继承 Go 的 `any`，用于异构集合及其他 Go 兼容场景 |

`null` 不是关键字，作用域与遮蔽规则沿用 Go 的 `nil`。Mao 的预声明环境不再提供 `nil`；如果用户自行声明该名称，按普通标识符处理。Go 的预声明函数 `delete` 和 `clear` 保持有效。`get`、`has`、`Delete`、`DeleteAt` 和 `clear` 是原生集合上的上下文操作，不是关键字，也不占用普通类型的方法名；实际声明的同名方法优先。

### 2.2 中文标点兼容

Mao 词法分析器在代码记号中接受以下中文标点：

| 中文标点 | 等价 ASCII 记号 | 用途示例 |
|---|---|---|
| `。` | `.` | `fmt。Println(value)` |
| `【` | `[` | `items【index】` |
| `】` | `]` | `int【】` 与 `values【key】` |
| `；` | `;` | `value := 1； next := 2` |
| `，` | `,` | `[1，2，3]` 与 `call(a，b)` |
| `《` | `<` | `Result《string》` 与 `a《b` |
| `》` | `>` | `Result《string》` 与 `a》b` |
| `？` | `?` | 保持与 ASCII `?` 相同的词法结果 |
| `“` 与 `”` | `"` 与 `"` | `message := “Mao”` |
| `‘` 与 `’` | `'` 与 `'` | `letter := ‘猫’` |
| `～` | `~` | 类型约束 `～int` |

转换由词法分析器把中文标点识别为相应记号完成，不得在解析前对源码进行全文字符串替换。`“…”` 作为 Go 解释字符串的中文定界形式，`‘…’` 作为 Go rune 字面量的中文定界形式；内容遵循 Go 转义和 rune 数量规则，生成 Go 时由格式化器改写为 ASCII 引号并正确转义内容。

已经位于 ASCII 字符串、中文引号字符串、字符字面量、原始字符串或注释内部的其他中文标点保持原样：

```mao
text := "【】。；，《》？～"
```

`？` 只转换成 ASCII `?`，不新增三元运算符、可选链或空值合并语义；由于 Go 当前没有 `?` 语法，它在没有其他明确语法规则时仍产生普通语法错误。`mao fmt` 将所有受支持的中文代码标点统一输出为对应 ASCII 形式，同时保持字符串内容和注释不变。

### 2.3 声明、类型与泛型

```mao
name := "Mao"                    // 推断为 string
int age = 3                      // 显式类型
const maxLives = 9               // 只有 const 是常量

string[] names                   // Go: []string
int[3] scores                    // Go: [3]int
string:int[] ages                // Go: map[string]int
Result<string> result            // Go: Result[string]
```

类型后缀按照从右向左的确定规则解析：

| Mao 类型 | 生成的 Go 类型 |
|---|---|
| `T[]` | `[]T` |
| `T[N]` | `[N]T` |
| `K:V[]` | `map[K]V` |
| `string:int[][]` | `map[string][]int` |
| `(string:int[])[]` | `[]map[string]int` |
| `string:(int:bool[])[]` | `map[string]map[int]bool` |
| `Generic<T>` | `Generic[T]` |

括号只用于消除复合类型的结合歧义，不改变底层 Go 类型。

### 2.4 浮点类型

`float` 是 Mao 的标准浮点类型，固定生成 Go `float64`：

```mao
float price = 3.14
ratio := 0.5       // 默认推断为 float
```

为兼容 Go 接口，Mao 仍允许显式使用 `float32` 和 `float64`。从 `float` 或 `float64` 转为 `float32` 可能损失精度，因此必须显式写 `float32(value)`；编译器不得静默缩窄。无类型浮点常量可按照 Go 的常量表示规则赋给目标浮点类型。

### 2.5 统一集合字面量

线性集合和字典共用 `[...]`：

```mao
numbers := [1, 2, 3]                  // 推断为 int[]
int[3] fixed = [1, 2, 3]             // 由目标类型生成数组
ages := ["cat": 3, "dog": 5]         // 推断为 string:int[]
mixed := ["cat":, "dog": 5]          // "cat" 的值为 null
items := [1, "cat", true, null]       // 推断为 any[]
profile := ["name": "Mao", "age": 3] // 推断为 string:any[]
emptyAges := [:]                      // 需要目标类型或显式类型
```

第一版采用以下无歧义规则：

1. 字面量中没有 `:` 时，它是线性集合；无目标类型时生成切片，有数组目标类型时生成固定数组。
2. 只要任意一项出现 `:`，整个字面量就是字典；其中未写 `:` 的项以及写成 `key:` 的项都表示 `key:null`。
3. 空字典写作 `[:]`。它必须具有目标类型，不能脱离上下文推断键和值的类型。
4. 项分隔符接受中文逗号 `，` 和 ASCII 逗号 `,`；格式化器统一输出 ASCII 逗号。
5. 字典使用 Go 原生 `map`：键必须可比较、键唯一、迭代顺序不作保证；可在编译期确定的重复键直接报错。
6. `dict(values)` 把数组或切片元素转换为字典键，返回 `T:any[]`，每个键的值都是 `null`；元素类型必须可比较，否则产生编译错误。
7. 无目标类型时，同类型元素保留能够容纳全部元素的精确共同类型；不存在精确共同类型时，线性集合推断为 `any[]`。
8. 字典分别推断键和值的共同类型。键或值不存在精确共同类型时，对应类型推断为 `any`；显式目标类型始终优先于自动推断。
9. `null` 参与无目标类型的线性集合或字典值推断时，对应元素类型或值类型使用 `any`，除非其他元素能够根据显式目标类型接收 `null`。
10. 推断为 `any` 的字典键仍必须遵守 Go 的可比较性要求。编译器拒绝能够静态确定为不可比较的键；无法在编译期确定的动态值沿用 Go 的运行时检查和 panic 行为。

统一的是 Mao 的字面量写法和操作方式，不是运行时存储结构。编译器仍根据目标类型生成 Go 数组、切片或 `map`，不增加容器包装对象。

### 2.6 `null`

Mao 源码只暴露 `null`，不暴露 `nil`：

```mao
values := ["cat": null, "dog": 5]
```

- 字典项省略值与显式写 `null` 完全等价。
- 没有目标类型且包含 `null` 的字典，其值类型推断为 `any`。
- 有目标类型时，`null` 只能赋给 Go 中可为空的类型：指针、切片、映射、通道、函数和接口。
- 向整数、浮点数、布尔、字符串、结构体或数组赋 `null` 是编译错误。
- `null` 在 Mao 的预声明环境中占据 Go `nil` 的位置，具有相同的作用域和可遮蔽规则，不是关键字。
- 生成的 Go 可以在内部使用 `nil`，但诊断信息和 Mao 格式化结果只使用 `null`。

### 2.7 集合读取与删除

```mao
value := values[key]                 // 键不存在时返回值类型的零值
value := values.get(key, defaultValue) // 仅在键不存在时求值并使用默认值
exists := values.has(key)            // 即使对应值为 null，键存在仍返回 true
values[key] = value                  // 新增或替换
delete(values, key)                  // 保留 Go 原有字典删除
values.Delete(key)                   // 按字典键删除
values.clear()                       // 字典删除全部键值对

items.Delete(value)                  // 删除第一个相等的切片元素
items.DeleteAt(index)                // 删除指定位置的切片元素
items.clear()                        // 元素归零，切片长度不变
```

第一版在保留 Go 原有接口的基础上增加集合方法：

- `values[key]` 完全沿用 Go 的单值索引语义：键不存在时返回值类型的零值，不抛出 Mao 专有错误。
- Go 的 `value, ok := values[key]` 双值查询是明确替换项，在 `.mao` 中不接受；读取默认值使用 `get`，判断键是否存在使用 `has`。
- `delete(values, key)` 保持 Go 原有参数限制、泛型规则、无返回值语义和缺失键静默忽略行为。
- 字典的 `Delete(key)` 是新增的方法式写法，生成 Go `delete(map, key)`；字典为 `null` 或键不存在时静默忽略。
- 切片的 `Delete(value)` 删除从左到右第一个相等元素；找不到时静默忽略，不返回结果。
- 切片的 `Delete(value)` 只适用于元素类型可比较的切片。元素类型不可比较时产生编译错误，避免引入反射相等规则。
- `any` 在静态类型层面可比较，但其动态值仍遵守 Go 的接口比较规则；动态值为切片、字典或函数时，`Delete` 中的相等比较会产生 Go 运行时 panic。
- 切片的 `DeleteAt(index)` 按从零开始的索引删除一个元素；索引越界时沿用 Go 的运行时 panic。
- 切片的 `Delete` 和 `DeleteAt` 必须作用于可重新赋值的切片存储位置，以便更新切片长度；临时切片表达式不能调用。
- 固定数组长度不可改变，因此固定数组不支持 `Delete` 或 `DeleteAt`。
- 字典的 `clear()` 删除全部键值对；切片的 `clear()` 把所有元素设置为对应零值，但不改变长度和容量。两者均直接生成 Go `clear`。
- 不支持 `del`、`remove`、`pop` 或其他备用删除接口。
- 已有字典键的值为 `null` 时，`get` 返回 `null`，`has` 返回 `true`。

`Delete`、`DeleteAt`、`get`、`has` 和 `clear` 是编译器识别的原生集合方法。用户自定义类型可以声明同名方法并按普通 Go 方法解析，实际声明的方法优先于内置集合方法；只有没有同名实际方法且接收对象底层类型为原生字典或切片时，才使用 Mao 的内置语义。

生成代码必须保证接收对象、键、索引和删除值各求值一次。`get` 的默认值表达式只在键不存在时求值，不得为满足代码生成便利而提前求值。字典 `Delete` 直接生成 Go `delete`。切片 `DeleteAt` 生成 `slices.Delete(items, index, index+1)` 并把结果重新赋给接收对象。切片 `Delete` 先查找第一个相等元素，再以相同方式删除该位置。字典和切片的 `clear()` 均生成 Go `clear`。

### 2.8 自动推断与转换

第一版的自动转换以不静默丢失信息为边界：

- 字面量根据目标类型进行 Go 允许的常量表示转换。
- 相同符号类别的定宽整数变量可以自动转换到位宽更大的类型，例如 `int8` 到 `int16`、`uint16` 到 `uint32`。
- `int8`、`int16` 和 `int32` 可以自动转换为 `int64`；`uint8`、`uint16` 和 `uint32` 可以自动转换为 `uint64`。
- `int` 可以自动转换为 `int64`，`uint` 和 `uintptr` 可以自动转换为 `uint64`。固定宽度整数不得自动转换为依赖目标架构宽度的 `int`、`uint` 或 `uintptr`。
- `float32` 可自动扩宽为 `float`/`float64`。
- 整数变量转换为 `float`、有符号与无符号整数互转、浮点转整数以及 `float` 转 `float32` 必须显式转换。
- 不同的命名类型之间沿用 Go 的显式转换要求，即使它们的底层整数类型满足扩宽条件；类型别名按其所指类型处理。
- 字符串与字节、字符序列之间沿用 Go 的显式转换规则。
- 无法证明安全的自动转换产生编译错误，诊断信息给出所需的显式转换写法。

这些自动转换规则统一适用于赋值、实参传递和返回值检查，不改变算术表达式的 Go 类型规则。

## 3. 与 Go、Lua 及其他语言的定位对照

| 能力 | Mao | Go | Lua 5.x | Python |
|---|---|---|---|---|
| 类型系统 | 静态类型 | 静态类型 | 动态类型 | 动态类型 |
| 显式类型位置 | `int age` | `var age int` | 不声明静态类型 | `age: int` 类型注解 |
| 推断变量 | `age := 3` | `age := 3` | `age = 3` | `age = 3` |
| 默认浮点 | `float`，对应 64 位 | `float64` | `number` 的浮点表示通常为双精度 | `float`，通常为 64 位 |
| 线性集合 | `int[]`，生成 Go 切片 | `[]int` | `table` 的整数键部分 | `list[int]` |
| 字典 | `string:int[]`，生成 Go `map` | `map[string]int` | `table` 的任意非 `nil` 键部分 | `dict[str, int]` |
| 异构集合 | 通过 `any` | 通过 `any` | `table` 原生支持 | 原生支持 |
| 缺失键读取 | 值类型零值 | 值类型零值 | `nil` | 抛出 `KeyError` |
| 带默认值读取 | `m.get(k, d)` | 双值查询后自行选择 | `m[k]` 配合条件判断 | `m.get(k, d)` |
| 是否存在 | `m.has(k)` | `_, ok := m[k]` | 通常用 `m[k] ~= nil` | `k in m` |
| 字典按键删除 | `m.Delete(k)` 或 `delete(m, k)` | `delete(m, k)` | `m[k] = nil` | `del m[k]` |
| 线性集合按位置删除 | `s.DeleteAt(i)` | `slices.Delete(s, i, i+1)` | `table.remove(s, i)` | `del s[i]` |
| 空值 | `null` | `nil` | `nil` | `None` |

Lua 的 `table` 同时承担数组、列表、集合、对象和字典等用途；所谓数组通常只是从整数键 `1` 开始连续存储的 `table`。给某个键赋 `nil` 会移除该键，因此 Lua 的 `table` 不能把 `nil` 作为可区分于“键不存在”的普通值保存。

Mao 不复制 Lua 或其他语言的完整对象模型。Mao 统一的是集合字面量和操作接口，类型检查和生成结果仍区分 Go 数组、切片与 `map`；方法式集合接口是编译期语法，不创建包装对象。

## 4. 编译器架构

### 4.1 工具链

第一版使用 Go 实现以下命令：

```text
mao build [packages]
mao run <file-or-package>
mao test [packages]
mao fmt [files]
mao check [packages]
mao emit-go [files]
```

处理流程固定为：

```text
.mao 源码
→ 词法分析
→ Mao 抽象语法树
→ 名称解析与类型检查
→ Mao 特性降级
→ Go 抽象语法树
→ go/format 生成 Go
→ 官方 go build/run/test
```

不得通过正则表达式或文本替换实现语言转换。类型前置、嵌套复合类型、统一字面量和字典方法都必须经过语法树与类型检查阶段。

### 4.2 包和 Go 互操作

- `.mao` 与 `.go` 文件可以位于同一 Go 包。
- Mao 可以直接 `import` Go 模块并调用其导出标识符。
- 生成代码保持 Go 包名、导入路径、导出规则、结构体布局和函数调用约定。
- 构建缓存放在项目临时目录中，不把生成的 `.go` 文件混入用户源目录。
- 生成代码写入 `//line` 指令，使 Go 编译错误和运行时堆栈尽量指回 `.mao` 文件。
- `mao emit-go` 用于查看生成结果和诊断编译问题。
- “兼容 Go”指生态、类型和包级互操作；由于 Mao 改用了类型前置与尖括号泛型，不承诺把任意 `.go` 文件改名为 `.mao` 后仍可解析。

### 4.3 运行时支持

第一版确定的功能均可直接生成原生 Go，不要求 Mao 项目链接专用运行时包：

`get`、`has`、`Delete`、`DeleteAt`、`clear` 直接生成内联 Go 代码，不为每次操作创建包装对象。若后续版本出现 Go 无法直接表达的行为，再单独评估运行时包；第一版不得为尚不存在的需求预先加入运行时依赖。

## 5. 实施阶段

### 阶段一：语法与生成闭环

- 建立 `mao` 命令、词法分析器、解析器、抽象语法树和位置映射。
- 实现全部中文标点等价记号、中文字符串与 rune 定界符，以及对应的源码位置跟踪。
- 支持包、导入、函数、控制流、表达式及 Go 基础声明。
- 实现类型前置、`T[]`、`T[N]`、`K:V[]` 和 `Generic<T>`。
- 生成格式正确且可由当前 Go 工具链编译的代码。

完成标准：包含 Mao 与 Go 文件的同包示例能够通过 `mao build`，生成代码可由 `gofmt` 无修改接受。

### 阶段二：集合、`null` 与字典操作

- 实现统一集合字面量的语境判定和类型推断。
- 实现同类型与异构集合推断、省略值、`null`、`any`、`dict` 转换及对应错误诊断。
- 实现索引读取、`get`、`has`、`Delete`、`DeleteAt`、`clear` 的类型检查与代码生成。

完成标准：值为 `null`、键不存在、数组目标、切片目标、嵌套字典和 Go 原生 `map` 互操作均通过行为测试。

### 阶段三：推断、转换与开发工具

- 按安全转换规则实现赋值、参数和返回值检查。
- 实现 `float` 到 Go `float64` 的完整映射。
- 完成 `mao fmt`、`mao check`、`mao test`、`mao emit-go`。
- 校正 `.mao` 源码位置、错误信息和运行时堆栈。

完成标准：错误均指向 Mao 源码；格式化结果稳定；同一输入重复生成完全一致的 Go 代码。

### 阶段四：发布准备

- 编写语言规范、Go 对照迁移指南和最小示例项目。
- 建立 Linux、macOS、Windows 的构建与测试流程。
- 固定第一版语法，新增解析快照和兼容性测试。
- 狸花猫形象只用于名称、图标和文档，不进入编译器语义。

## 6. 测试与验收

### 6.1 解析和格式化

- 覆盖所有前置类型及多层嵌套组合。
- 覆盖 `。→.`、`【→[`、`】→]`、`；→;`、`，→,`、`《→<`、`》→>`、`？→?`、`～→~`，验证 `mao fmt` 统一输出 ASCII 标点。
- 覆盖 `“…”` 字符串和 `‘…’` rune，验证转义、Unicode、非法闭合及 rune 数量诊断。
- 验证字符串、字符字面量、原始字符串和注释中的中文标点不被转换。
- 覆盖线性、字典、混合、省略值及空字典字面量。
- 验证 `mao fmt` 幂等：连续运行两次内容不再变化。

### 6.2 类型检查

- 验证无目标类型、目标数组、目标切片和目标字典的推断。
- 验证同类型集合保留精确类型，异构线性集合推断为 `any[]`，异构字典独立推断键和值类型。
- 验证显式目标类型优先于异构集合的默认 `any` 推断。
- 验证 `float`、`float32`、`float64` 的扩宽和禁止缩窄。
- 验证每一条整数扩宽边界、禁止跨有符号与无符号自动转换，以及不同命名类型必须显式转换。
- 验证 `null` 只能进入可为空类型。
- 验证字典键可比较、常量重复键、空字典缺少类型等错误。
- 验证 `get` 的默认值与字典值类型兼容。
- 验证静态可知的不可比较值不能作为 `any` 字典键。
- 验证 `dict` 拒绝元素类型不可比较的数组和切片。

### 6.3 运行行为

- `values[key]` 缺失时返回字典值类型的零值，与 Go 行为一致。
- `has` 能区分缺失键与值为 `null` 的已有键。
- `get` 只在键缺失时求值默认值表达式；使用具有可观察副作用的默认值函数验证。
- `value, ok := values[key]` 在 `.mao` 中产生编译错误，并提示分别使用 `get` 或 `has`。
- `delete(values, key)` 保持 Go 原有行为。
- `values.Delete(key)` 删除已有字典项；字典为 `null` 或键不存在时静默忽略。
- `items.Delete(value)` 只删除第一个相等元素；重复值保留其余项，找不到时切片不变。
- `items.DeleteAt(index)` 删除指定位置；分别验证首项、末项、空切片和越界行为。
- 整数切片中 `Delete(1)` 按值删除整数 `1`，`DeleteAt(1)` 按位置删除索引 `1`，两者不得混淆。
- 不可比较元素切片调用 `Delete`、固定数组调用 `Delete` 或 `DeleteAt` 时产生编译错误。
- `any[]` 的 `Delete` 对可比较动态值正常工作，对不可比较动态值沿用 Go 接口比较的 panic 行为。
- 任何类型使用 `del`、`pop` 或 `remove` 均按未声明标识符或普通成员解析，不提供内置语义。
- 字典 `clear()` 删除全部条目；对 `null` 字典调用时静默完成。
- 切片 `clear()` 将全部元素设置为零值，但保持长度和容量不变；对 `null` 切片调用时静默完成。
- `dict(slice)` 生成以元素为键、值为 `null` 的原生 Go `map`。

### 6.4 Go 兼容

- Mao 调用 Go 标准库、第三方模块和泛型 API。
- Go 文件调用同包 Mao 生成的导出函数与类型。
- Go 数组、切片和 `map` 在边界处不需要适配器。
- `go test`、竞态检测和基准测试能够作用于生成后的包。
- 编译错误和 panic 的位置能够映射回 `.mao` 源文件。

## 7. 第一版边界

第一版不包含宏、运算符重载、自定义容器运行时、垃圾回收器、独立包管理器、Go 编译器分支或编辑器语言服务器。编辑器支持在语法和编译接口稳定后单独规划。

第一版保持静态类型，不引入 Lua 式唯一 `table` 类型、动态装箱值或统一集合运行时。需要容纳不同类型时使用 Go 的 `any`；集合在类型检查和生成后仍是原生 Go 数组、切片或 `map`。

Mao 以 Go 语法和语义为基线。计划书只列出 Mao 明确增加或替换的部分；没有明确修改的 Go 关键字、表达式、语句、预声明标识符和运行行为继续有效。

第一版明确增加或替换的语法如下：

- 线性集合和字典使用统一方括号字面量；是否生成 Go 数组、切片或 `map` 由字面量形式和目标类型决定。
- 无法推断出精确共同类型的异构集合使用 Go `any`，不引入新的动态值类型。
- 字典在保留 `delete(values, key)` 的同时，新增 `values.Delete(key)`。
- 字典单值索引沿用 Go 的零值行为，但 Go 的双值查询写法替换为 `get` 和 `has`。
- 切片新增按值删除 `items.Delete(value)` 和按位置删除 `items.DeleteAt(index)`；不增加 `del`、`remove` 或 `pop`。
- 字典和切片新增方法式 `clear()`，语义分别对应 Go `clear(map)` 和 `clear(slice)`；Go 的预声明函数 `clear` 仍然有效。
- 新增 `dict(values)`，把可比较的数组或切片元素转换为字典键，并将每个值设置为 `null`。
- Mao 源码以 `null` 替换预声明空值 `nil`；生成的内部 Go 代码仍使用 `nil`。
- 新增固定对应 Go `float64` 的 `float`，不增加上下文相关的可变宽度浮点类型。
- 赋值、实参和返回值允许计划所列的无损整数扩宽及 `float32` 到 `float`/`float64` 的扩宽；其他数值转换沿用显式转换要求。
- 复合类型使用 Mao 的前置写法，不同时接受 Go 的后置复合类型；这一项是对 `.mao` 类型语法的明确替换。
- 泛型实例化使用 `Generic<T>`，不同时接受 Go 的 `Generic[T]`；同一项目中的 `.go` 文件继续使用标准 Go 语法。
- 代码记号接受计划所列中文标点并格式化为对应 ASCII 形式；这些等价记号不改变对应 Go 记号的语法和语义。

除上述明确差异外，生成的内部 Go 代码和未修改的 Mao 表面语法均沿用 Go。
