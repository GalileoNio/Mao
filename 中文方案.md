# Mao 中文命名方案

## 1. 目标

本方案作为 `PLAN.md` 的独立补充，不修改原有语言方案。

Mao 面向中文开发者提供完整的中文书写形式，同时保持到 Go 的确定转换能力：

- 所有保留字具有唯一中文名称。
- 所有预声明常量、类型和函数具有中文名称。
- Mao 支持的每一个 Go 公开标准库包具有中文虚拟导入路径。
- 标准库公开成员可以使用中文名称。
- 中文名称与对应 Go 名称指向同一语义对象，不形成两套运行时接口。
- 第三方 Go 包继续保持兼容，但编译器不擅自翻译第三方接口。

Go 没有语言层面的“内置包”。本方案所称的“内置包”统一指 Mao 所支持 Go 版本中可由用户导入的公开标准库包。

## 2. 总体规则

Mao 采用“中文规范名、英文兼容名、单一内部语义”的名称系统：

```text
中文 Mao 源码
→ 词法分析并识别中文保留字
→ 名称解析并绑定中文预声明符号
→ 根据名称清单绑定标准库包和公开成员
→ Mao 抽象语法树
→ Go 抽象语法树
→ 英文 Go 源码
```

不得通过正则表达式或全文字符串替换完成中文转换。字符串、注释、原始字符串、用户声明的普通标识符以及第三方包中的同名成员必须保持原样。

建议提供三种格式化模式：

| 命令 | 结果 |
|---|---|
| `mao fmt` | 输出中文规范名 |
| `mao fmt --names=preserve` | 保留源码原有的中文或英文名称 |
| `mao fmt --names=en` | 输出英文兼容名 |

`mao emit-go` 始终生成合法的英文 Go 源码。

## 3. 保留字

Go 的 25 个关键字在 Mao 中均应有中文规范名。中文名和英文名解析为相同的内部记号。下表是当前候选，其中 `for` 暂列 `为`、`对`两个方案，确定后只保留一个规范名。

| 中文规范名 | 英文兼容名 | 用途 |
|---|---|---|
| `止` | `break` | 结束当前循环或选择语句 |
| `本` | `default` | 默认分支 |
| `方` | `func` | 声明函数或函数类型 |
| `接` | `interface` | 声明接口类型 |
| `择` | `select` | 选择可执行的通道操作 |
| `例` | `case` | 声明选择分支 |
| `延` | `defer` | 延后执行函数调用 |
| `去` | `go` | 启动并发函数调用 |
| `图` | `map` | Mao 现有方案中的 Go 原生映射转换 |
| `构` | `struct` | 声明结构类型 |
| `槽` | `chan` | 声明通道类型 |
| `另` | `else` | 条件的后续分支 |
| `转` | `goto` | 跳转到标签 |
| `包` | `package` | 声明源码包 |
| `选` | `switch` | 按表达式或类型选择分支 |
| `恒` | `const` | 声明常量 |
| `贯` | `fallthrough` | 继续执行下一个选择分支 |
| `若` | `if` | 条件语句 |
| `遍` | `range` | 遍历集合或 Go 原生可遍历值 |
| `类` | `type` | 声明类型 |
| `续` | `continue` | 进入下一次循环 |
| `为` / `对` | `for` | 循环语句；中文规范名待定 |
| `舶` | `import` | 导入包 |
| `返` | `return` | 从函数返回 |
| `变` | `var` | Go 互操作所需的变量声明形式 |

中文规范名与英文兼容名都属于保留字，不能用作普通标识符。

## 4. 预声明标识符

预声明标识符位于 Mao 的预声明作用域，不属于保留字。其遮蔽规则与对应 Go 标识符一致。

### 4.1 常量与空值

| 中文规范名 | 英文兼容名 |
|---|---|
| `真` | `true` |
| `假` | `false` |
| `序` | `iota` |
| `空` | `null` |

`空` 对应 Mao 原计划中的 `null`，不直接暴露 Go 的 `nil`。

### 4.2 类型

| 中文规范名 | 英文兼容名 |
|---|---|
| `微` / `浮` | `float` |
| `表` | `table` |
| `凡` | `any` |
| `阴阳` | `bool` |
| `文` | `string` |
| `字节` | `byte` |
| `字` | `rune` |
| `可比` | `comparable` |
| `误` | `error` |
| `整` | `int` |
| `整8` | `int8` |
| `整16` | `int16` |
| `整32` | `int32` |
| `整64` | `int64` |
| `无符整` | `uint` |
| `无符整8` | `uint8` |
| `无符整16` | `uint16` |
| `无符整32` | `uint32` |
| `无符整64` | `uint64` |
| `指针整数` | `uintptr` |
| `微32` / `浮32` | `float32` |
| `微64` / `浮64` | `float64` |
| `复64` / `杂64` / `重64` / `络64` | `complex64` |
| `复128` / `杂128` / `重128` / `络128` | `complex128` |

`float` 暂列 `微`、`浮`两个方案，`float32` 和 `float64` 跟随采用相同字根；`complex` 暂列 `复`、`杂`、`重`、`络`四个方案，`complex64` 和 `complex128` 跟随采用相同字根。确定后分别只保留一个中文规范名。

### 4.3 预声明函数

| 中文规范名 | 英文兼容名 |
|---|---|
| `补` | `append` |
| `容` | `cap` |
| `清` | `clear` |
| `闭` | `close` |
| `复` / `杂` / `重` / `络` | `complex` |
| `抄` / `拷` | `copy` |
| `删` | `delete` |
| `虚` | `imag` |
| `长` | `len` |
| `拟` / `制` / `造` / `做` | `make` |
| `最大` | `max` |
| `最小` | `min` |
| `新` | `new` |
| `恐` | `panic` |
| `印` | `print` |
| `印行` | `println` |
| `实` | `real` |
| `重` / `复` | `recover` |

`copy` 暂列 `抄`、`拷`两个方案，`make` 暂列 `拟`、`制`、`造`、`做`四个方案。`recover` 的英文名称不是 `revovery` 或 `recovery`。`complex` 与 `recover` 的候选存在 `复`、`重`交集，最终不能选择同一个中文名：如果 `complex` 选择 `复`，`recover` 只能选择 `重`；如果 `complex` 选择 `重`，`recover` 只能选择 `复`。

### 4.4 `table` 上下文操作

这些名称不是全局保留字。只有在接收对象已经确定为 Mao `table` 时，编译器才赋予内置语义。

| 中文规范名 | 英文兼容名 |
|---|---|
| `取` | `get` |
| `有` | `has` |
| `于` | `at` |
| `键全` | `keys` |
| `值全` | `values` |
| `删` | `Delete` |
| `删于` | `DeleteAt` |
| `清` | `clear` |

用户类型实际声明的同名方法优先，不应被编译器改写成 `table` 操作。

## 5. 标准库中文导入

### 5.1 中文导入路径

中文标准库直接使用对应的中文路径，不增加统一前缀：

```mao
包 main

舶 (
    "范"
    "网/超文本"
    "编码/JSON"
)

方 main() {
    范.打印行("你好，Mao")
}
```

生成的 Go 源码分别导入 `fmt`、`net/http` 和 `encoding/json`。这些中文路径由 Mao 标准库名称清单确定转换关系，不对应真实的 Go 模块路径，也不交给 Go 模块代理下载。

### 5.2 常用包命名

| Mao 虚拟导入路径 | Go 导入路径 | 默认本地包名 |
|---|---|---|
| `范` | `fmt` | `范` |
| `系统` | `os` | `系统` |
| `出入` | `io` | `出入` |
| `缓冲输入输出` | `bufio` | `缓冲输入输出` |
| `文具` | `strings` | `文具` |
| `字节` | `bytes` | `字节工具` |
| `文译` | `strconv` | `文译` |
| `时` | `time` | `时` |
| `数` | `math` | `数` |
| `数/随机` | `math/rand/v2` | `随机` |
| `同` | `sync` | `同` |
| `境` | `context` | `境` |
| `误具` | `errors` | `误具` |
| `志` | `log` | `志` |
| `正则` | `regexp` | `正则` |
| `排` | `sort` | `排` |
| `切具` | `slices` | `切具` |
| `图具` | `maps` | `图具` |
| `径` | `path` | `径` |
| `径/档径` | `path/filepath` | `档径` |
| `数据库/SQL` | `database/sql` | `数据库` |
| `编码/JSON` | `encoding/json` | `JSON` |
| `网` | `net` | `网` |
| `网/超文本` | `net/http` | `超文本` |
| `测试` | `testing` | `测试` |

此表用于说明命名原则，不代替完整标准库清单。正式发布时，Mao 所支持 Go 版本的每一个可导入公开标准库包都必须具有唯一中文虚拟路径。

## 6. 标准库成员中文化

只翻译包名会产生如下不完整形式：

```mao
范.Println("你好")
```

因此，标准库的公开常量、变量、函数、类型、字段和方法也需要中文名称：

```mao
范.打印行("你好")
超文本.监听并服务(":8080", 处理器)
JSON.编码(数据)
```

名称转换直接绑定到 Go 类型系统识别出的导出对象。例如：

```text
范.打印行 → fmt.Println
```

编译器不得生成一层中文包装函数。这样可以保持：

- 与原 Go 标准库相同的调用行为。
- 与 Go 相同的泛型、接口和方法集合。
- 不增加运行时调用层级。
- Go 文档、性能分析和堆栈仍能对应原始标准库符号。

在某个标准库成员尚无中文名称时，可以临时使用英文成员名。只有当一个包的全部公开对象均通过覆盖检查后，该包才能标记为“完整中文化”。

## 7. 版本化名称清单

编译器发行包应包含机器可读的标准库名称清单。每项至少记录：

```text
Mao 虚拟导入路径
Go 导入路径
中文默认本地包名
Go 原始包名
支持的 Go 版本
包级公开对象映射
公开类型的字段和方法映射
```

中文名称由人工确认，不使用机器翻译或拼音自动生成。工具负责读取目标 Go 版本的导出数据并校验清单。

升级 Go 版本时必须执行：

1. 枚举目标版本全部可导入的公开标准库包。
2. 检查每个包均有唯一中文虚拟路径。
3. 枚举每个包的全部公开对象。
4. 检查完整中文化包不存在遗漏、重复或已经失效的成员。
5. 补齐清单后才允许发布支持该 Go 版本的 Mao 工具链。

## 8. 名称冲突规则

1. 中文保留字在词法阶段识别，不能作为普通标识符。
2. 预声明中文名称可以被局部声明遮蔽。
3. 中文名与对应英文名在同一作用域指向同一声明时，不允许重复声明或重复导入。
4. 标准库默认本地包名不能与保留字冲突。
5. 包名与预声明类型容易混淆时使用统一的 `具` 后缀，例如 `误具`、`文具`、`图具`。
6. 用户显式指定导入别名时，以用户别名为准，不再自动增加其他同义名。
7. 标准库成员映射只作用于已经确认来源的标准库对象。
8. 用户包、用户类型和第三方包中的同名成员保持原样。
9. 格式化器只转换已经完成语法和名称解析的符号；无法确认语义的文本保持原样。

## 9. 第三方包

第三方 Go 包不由 Mao 编译器自动翻译。用户可以使用中文导入别名：

```mao
舶 数据库 "github.com/example/database"
```

成员名仍按第三方包原始接口书写。包作者可以发布 Mao 名称清单，为自己的包提供中文成员名称。该清单必须声明：

- 原始 Go 导入路径。
- 适用版本范围。
- 中文名称与 Go 导出对象的确定映射。

编译器不得根据第三方成员的英文拼写自动生成中文接口。

## 10. 诊断与源码位置

- 诊断信息优先显示中文规范名。
- 涉及 Go 互操作时同时显示原始名称，例如 `范.打印行（fmt.Println）`。
- 源码位置按照原始 UTF-8 文件记录。
- 生成 Go 后使用 `//line` 指令把编译错误和运行时堆栈映射回 `.mao` 文件。
- 字符串、注释和原始字符串中的中文名称不参与转换。

## 11. 实施顺序

### 第一阶段：语言名称

- 实现 25 个中文保留字。
- 实现全部预声明标识符的中文名称。
- 中文名和英文名归一化为同一抽象语法树语义。
- 实现三种格式化名称模式。

### 第二阶段：标准库包

- 确定 Mao 支持的 Go 版本。
- 建立完整的标准库包路径清单。
- 实现标准库中文路径解析。
- 实现包覆盖检查和名称冲突检查。

### 第三阶段：标准库成员

- 按使用频率逐包确定中文成员名。
- 通过 Go 导出数据绑定真实符号。
- 覆盖常量、变量、函数、类型、字段和方法。
- 建立公开成员覆盖检查。

### 第四阶段：第三方扩展

- 定义第三方 Mao 名称清单格式。
- 校验导入路径和版本范围。
- 提供名称清单检查命令。

## 12. 验收条件

- 25 组中文保留字与英文兼容名产生相同语义。
- 全部预声明标识符具有中文名称并遵守遮蔽规则。
- 格式化器不会改写字符串、注释、用户符号或第三方符号。
- 支持的 Go 版本不存在缺少中文虚拟路径的公开标准库包。
- 标记为完整中文化的包不存在未映射的公开成员。
- 中文调用与英文 Go 调用绑定同一个导出对象。
- 同一输入重复格式化和生成代码得到完全一致的结果。
- 中文源码产生的错误能够准确定位到原始 `.mao` 文件。
