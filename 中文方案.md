# Mao 中文命名方案

## 1. 目标

本方案作为 `PLAN.md` 的独立补充，不修改原有语言方案。

Mao 面向中文开发者提供完整的中文书写形式，同时保持到 Go 的确定转换能力：

- 所有保留字具有唯一中文名称。
- 所有预声明常量、类型和函数具有中文名称。
- Mao 支持的每一个 Go 公开标准库包具有中文虚拟导入路径。
- 标准库公开成员可以使用中文名称。
- 中文名称与对应 Go 名称指向同一语义对象，不形成两套运行时接口。
- 第三方 Go 包继续保持兼容，但编译器不擅自翻译第三方接口。

Go 没有语言层面的“内置包”。本方案所称的“内置包”统一指 Mao 所支持 Go 版本中可由用户导入的公开标准库包。

## 2. 总体规则

Mao 采用“中文规范名、英文兼容名、单一内部语义”的名称系统：

```text
中文 Mao 源码
→ 词法分析并识别中文保留字
→ 名称解析并绑定中文预声明符号
→ 根据名称清单绑定标准库包和公开成员
→ Mao 抽象语法树
→ Go 抽象语法树
→ 英文 Go 源码
```

不得通过正则表达式或全文字符串替换完成中文转换。字符串、注释、原始字符串、用户声明的普通标识符以及第三方包中的同名成员必须保持原样。

建议提供三种格式化模式：

| 命令 | 结果 |
|---|---|
| `mao fmt` | 输出中文规范名 |
| `mao fmt --names=preserve` | 保留源码原有的中文或英文名称 |
| `mao fmt --names=en` | 输出英文兼容名 |

`mao emit-go` 始终生成合法的英文 Go 源码。

## 3. 保留字

Go 的 25 个关键字在 Mao 中均有唯一中文规范名。中文名和英文名解析为相同的内部记号。

| 中文规范名 | 英文兼容名 | 用途 |
|---|---|---|
| `跳出` | `break` | 结束当前循环或选择语句 |
| `默认` | `default` | 默认分支 |
| `函数` | `func` | 声明函数或函数类型 |
| `接口` | `interface` | 声明接口类型 |
| `监听` | `select` | 选择可执行的通道操作 |
| `分支` | `case` | 声明选择分支 |
| `延后` | `defer` | 延后执行函数调用 |
| `并发` | `go` | 启动并发函数调用 |
| `映射` | `map` | Mao 现有方案中的 Go 原生映射转换 |
| `结构` | `struct` | 声明结构类型 |
| `通道` | `chan` | 声明通道类型 |
| `否则` | `else` | 条件的后续分支 |
| `跳转` | `goto` | 跳转到标签 |
| `包` | `package` | 声明源码包 |
| `选择` | `switch` | 按表达式或类型选择分支 |
| `常量` | `const` | 声明常量 |
| `贯穿` | `fallthrough` | 继续执行下一个选择分支 |
| `如果` | `if` | 条件语句 |
| `遍历` | `range` | 遍历集合或 Go 原生可遍历值 |
| `类型` | `type` | 声明类型 |
| `继续` | `continue` | 进入下一次循环 |
| `循环` | `for` | 循环语句 |
| `导入` | `import` | 导入包 |
| `返回` | `return` | 从函数返回 |
| `变量` | `var` | Go 互操作所需的变量声明形式 |

中文规范名与英文兼容名都属于保留字，不能用作普通标识符。

## 4. 预声明标识符

预声明标识符位于 Mao 的预声明作用域，不属于保留字。其遮蔽规则与对应 Go 标识符一致。

### 4.1 常量与空值

| 中文规范名 | 英文兼容名 |
|---|---|
| `真` | `true` |
| `假` | `false` |
| `序号` | `iota` |
| `空值` | `null` |

`空值` 对应 Mao 原计划中的 `null`，不直接暴露 Go 的 `nil`。

### 4.2 类型

| 中文规范名 | 英文兼容名 |
|---|---|
| `浮点` | `float` |
| `表` | `table` |
| `任意` | `any` |
| `布尔` | `bool` |
| `文本` | `string` |
| `字节` | `byte` |
| `字符` | `rune` |
| `可比较` | `comparable` |
| `错误` | `error` |
| `整数` | `int` |
| `整数8` | `int8` |
| `整数16` | `int16` |
| `整数32` | `int32` |
| `整数64` | `int64` |
| `无符号整数` | `uint` |
| `无符号整数8` | `uint8` |
| `无符号整数16` | `uint16` |
| `无符号整数32` | `uint32` |
| `无符号整数64` | `uint64` |
| `指针整数` | `uintptr` |
| `浮点32` | `float32` |
| `浮点64` | `float64` |
| `复数64` | `complex64` |
| `复数128` | `complex128` |

### 4.3 预声明函数

| 中文规范名 | 英文兼容名 |
|---|---|
| `追加` | `append` |
| `容量` | `cap` |
| `清空` | `clear` |
| `关闭` | `close` |
| `组成复数` | `complex` |
| `复制` | `copy` |
| `删除` | `delete` |
| `虚部` | `imag` |
| `长度` | `len` |
| `创建` | `make` |
| `最大` | `max` |
| `最小` | `min` |
| `新建` | `new` |
| `异常` | `panic` |
| `打印` | `print` |
| `打印行` | `println` |
| `实部` | `real` |
| `恢复` | `recover` |

### 4.4 `table` 上下文操作

这些名称不是全局保留字。只有在接收对象已经确定为 Mao `table` 时，编译器才赋予内置语义。

| 中文规范名 | 英文兼容名 |
|---|---|
| `取值` | `get` |
| `存在` | `has` |
| `按位取项` | `at` |
| `全部键` | `keys` |
| `全部值` | `values` |
| `删除键` | `Delete` |
| `删除位置` | `DeleteAt` |
| `清空` | `clear` |

用户类型实际声明的同名方法优先，不应被编译器改写成 `table` 操作。

## 5. 标准库中文导入

### 5.1 虚拟导入路径

中文标准库使用 `标准/` 虚拟命名空间：

```mao
包 main

导入 (
    "标准/格式"
    "标准/网络/超文本"
    "标准/编码/JSON"
)

函数 main() {
    格式.打印行("你好，Mao")
}
```

生成的 Go 源码分别导入 `fmt`、`net/http` 和 `encoding/json`。`标准/...` 只由 Mao 工具链解析，不对应真实的 Go 模块路径。

采用虚拟命名空间可以避免以下问题：

- 中文标准库路径与用户自己的模块路径重名。
- Go 模块代理尝试下载不存在的中文模块。
- 编译器根据字符串内容猜测某个导入是否属于标准库。

### 5.2 常用包命名

| Mao 虚拟导入路径 | Go 导入路径 | 默认本地包名 |
|---|---|---|
| `标准/格式` | `fmt` | `格式` |
| `标准/系统` | `os` | `系统` |
| `标准/输入输出` | `io` | `输入输出` |
| `标准/缓冲输入输出` | `bufio` | `缓冲输入输出` |
| `标准/文本` | `strings` | `文本工具` |
| `标准/字节` | `bytes` | `字节工具` |
| `标准/文本转换` | `strconv` | `文本转换` |
| `标准/时间` | `time` | `时间` |
| `标准/数学` | `math` | `数学` |
| `标准/数学/随机` | `math/rand/v2` | `随机` |
| `标准/同步` | `sync` | `同步` |
| `标准/上下文` | `context` | `上下文` |
| `标准/错误` | `errors` | `错误工具` |
| `标准/日志` | `log` | `日志` |
| `标准/正则` | `regexp` | `正则` |
| `标准/排序` | `sort` | `排序` |
| `标准/切片` | `slices` | `切片工具` |
| `标准/映射` | `maps` | `映射工具` |
| `标准/路径/文件` | `path/filepath` | `文件路径` |
| `标准/数据库/SQL` | `database/sql` | `数据库` |
| `标准/编码/JSON` | `encoding/json` | `JSON` |
| `标准/网络/超文本` | `net/http` | `超文本` |
| `标准/测试` | `testing` | `测试` |

此表用于说明命名原则，不代替完整标准库清单。正式发布时，Mao 所支持 Go 版本的每一个可导入公开标准库包都必须具有唯一中文虚拟路径。

## 6. 标准库成员中文化

只翻译包名会产生如下不完整形式：

```mao
格式.Println("你好")
```

因此，标准库的公开常量、变量、函数、类型、字段和方法也需要中文名称：

```mao
格式.打印行("你好")
超文本.监听并服务(":8080", 处理器)
JSON.编码(数据)
```

名称转换直接绑定到 Go 类型系统识别出的导出对象。例如：

```text
格式.打印行 → fmt.Println
```

编译器不得生成一层中文包装函数。这样可以保持：

- 与原 Go 标准库相同的调用行为。
- 与 Go 相同的泛型、接口和方法集合。
- 不增加运行时调用层级。
- Go 文档、性能分析和堆栈仍能对应原始标准库符号。

在某个标准库成员尚无中文名称时，可以临时使用英文成员名。只有当一个包的全部公开对象均通过覆盖检查后，该包才能标记为“完整中文化”。

## 7. 版本化名称清单

编译器发行包应包含机器可读的标准库名称清单。每项至少记录：

```text
Mao 虚拟导入路径
Go 导入路径
中文默认本地包名
Go 原始包名
支持的 Go 版本
包级公开对象映射
公开类型的字段和方法映射
```

中文名称由人工确认，不使用机器翻译或拼音自动生成。工具负责读取目标 Go 版本的导出数据并校验清单。

升级 Go 版本时必须执行：

1. 枚举目标版本全部可导入的公开标准库包。
2. 检查每个包均有唯一中文虚拟路径。
3. 枚举每个包的全部公开对象。
4. 检查完整中文化包不存在遗漏、重复或已经失效的成员。
5. 补齐清单后才允许发布支持该 Go 版本的 Mao 工具链。

## 8. 名称冲突规则

1. 中文保留字在词法阶段识别，不能作为普通标识符。
2. 预声明中文名称可以被局部声明遮蔽。
3. 中文名与对应英文名在同一作用域指向同一声明时，不允许重复声明或重复导入。
4. 标准库默认本地包名不能与保留字冲突。
5. 包名与预声明类型容易混淆时使用明确后缀，例如 `错误工具`、`文本工具`、`映射工具`。
6. 用户显式指定导入别名时，以用户别名为准，不再自动增加其他同义名。
7. 标准库成员映射只作用于已经确认来源的标准库对象。
8. 用户包、用户类型和第三方包中的同名成员保持原样。
9. 格式化器只转换已经完成语法和名称解析的符号；无法确认语义的文本保持原样。

## 9. 第三方包

第三方 Go 包不由 Mao 编译器自动翻译。用户可以使用中文导入别名：

```mao
导入 数据库 "github.com/example/database"
```

成员名仍按第三方包原始接口书写。包作者可以发布 Mao 名称清单，为自己的包提供中文成员名称。该清单必须声明：

- 原始 Go 导入路径。
- 适用版本范围。
- 中文名称与 Go 导出对象的确定映射。

编译器不得根据第三方成员的英文拼写自动生成中文接口。

## 10. 诊断与源码位置

- 诊断信息优先显示中文规范名。
- 涉及 Go 互操作时同时显示原始名称，例如 `格式.打印行（fmt.Println）`。
- 源码位置按照原始 UTF-8 文件记录。
- 生成 Go 后使用 `//line` 指令把编译错误和运行时堆栈映射回 `.mao` 文件。
- 字符串、注释和原始字符串中的中文名称不参与转换。

## 11. 实施顺序

### 第一阶段：语言名称

- 实现 25 个中文保留字。
- 实现全部预声明标识符的中文名称。
- 中文名和英文名归一化为同一抽象语法树语义。
- 实现三种格式化名称模式。

### 第二阶段：标准库包

- 确定 Mao 支持的 Go 版本。
- 建立完整的标准库包路径清单。
- 实现 `标准/...` 虚拟路径解析。
- 实现包覆盖检查和名称冲突检查。

### 第三阶段：标准库成员

- 按使用频率逐包确定中文成员名。
- 通过 Go 导出数据绑定真实符号。
- 覆盖常量、变量、函数、类型、字段和方法。
- 建立公开成员覆盖检查。

### 第四阶段：第三方扩展

- 定义第三方 Mao 名称清单格式。
- 校验导入路径和版本范围。
- 提供名称清单检查命令。

## 12. 验收条件

- 25 组中文保留字与英文兼容名产生相同语义。
- 全部预声明标识符具有中文名称并遵守遮蔽规则。
- 格式化器不会改写字符串、注释、用户符号或第三方符号。
- 支持的 Go 版本不存在缺少中文虚拟路径的公开标准库包。
- 标记为完整中文化的包不存在未映射的公开成员。
- 中文调用与英文 Go 调用绑定同一个导出对象。
- 同一输入重复格式化和生成代码得到完全一致的结果。
- 中文源码产生的错误能够准确定位到原始 `.mao` 文件。
